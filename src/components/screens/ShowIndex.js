import MovieContainer from '../containers/MovieContainer';

const ShowIndex = ({ navigation, route }) => {
  return <MovieContainer navigation={navigation} route={route} />;
};

export default ShowIndex;
